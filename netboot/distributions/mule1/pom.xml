<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.mule.galaxy.netboot</groupId>
        <artifactId>netboot-distributions</artifactId>
        <version> 1.5-SNAPSHOT</version>
    </parent>
    <artifactId>mule1-netboot-distribution</artifactId>
    <packaging>pom</packaging>
    <name>Mule 1 NetBoot Distribution</name>

    <properties>
        <baseName>mule1-netboot</baseName>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <!--
                    2.2-beta-2 is broken due to a regression. Attempting
                    to configure permissions via multiple includes/excludes
                    adds duplicate entries to the assembly:

                    http://jira.codehaus.org/browse/MASSEMBLY-285

                    2.2-beta-1 is broken too:
                    http://jira.codehaus.org/browse/MASSEMBLY-194
                    Fixed in 2.2-beta-2, but it has a blocker regression in turn.

                    WT$%#^!
                -->
                <version>2.1</version>
                <!--<version>${mavenAssemblyPluginVersion}</version>-->
                <configuration>
                    <finalName>${baseName}-${version}</finalName>
                    <descriptors>
                        <descriptor>assembly.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>false</appendAssemblyId>
                    <tarLongFileMode>gnu</tarLongFileMode>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <!--
                                'attached' ensures there's only a single run bound to m2's lifecycle.
                                The default 'assembly' goal would run every plugin in this pom twice.
                            -->
                            <goal>attached</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.mule.tools</groupId>
                <artifactId>mule-assembly-verifier</artifactId>
                <!-- version is managed in the pluginManagement of the toplevel pom -->
                <executions>
                    <execution>
                        <phase>verify</phase>
                        <goals>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectOutputFile>${baseName}-${version}.zip</projectOutputFile>
                    <!-- Default is under src/main/resources, pull it to the root for this project -->
                    <whitelist>assembly-whitelist.txt</whitelist>
                    <!--
                        Blacklisted entries are supposed to be part of the distribution,
                        but not verified. They are defined by their full path relative to the
                        distribution *archive*; therefore we need to specify the name of the
                        toplevel subdirectory in the archive too.
                    -->
                    <blacklist>
                        <entry>${baseName}-${version}/lib/endorsed/xml-apis-1.3.04.jar</entry>
                        <entry>${baseName}-${version}/lib/endorsed/xercesImpl-2.9.1.jar</entry>
                        <entry>${baseName}-${version}/lib/endorsed/xml-serializer-2.7.1.jar</entry>
                        <entry>${baseName}-${version}/lib/endorsed/xalan-2.7.1.jar</entry>
                    </blacklist>
                </configuration>
            </plugin>


        </plugins>

    </build>

    <dependencies>
        <dependency>
            <groupId>org.mule.galaxy.netboot</groupId>
            <artifactId>mule-netboot-support</artifactId>
            <version>${version}</version>
        </dependency>

        <dependency>
            <groupId>org.mule.galaxy.netboot</groupId>
            <artifactId>mule-netboot-launcher</artifactId>
            <version>${version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-all</artifactId>
        </dependency>

        <dependency>
            <groupId>commons-httpclient</groupId>
            <artifactId>commons-httpclient</artifactId>
        </dependency>

        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>


        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl104-over-slf4j</artifactId>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
        </dependency>

        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
        </dependency>

    </dependencies>

</project>
